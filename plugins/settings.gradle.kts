/*
 * Copyright (c) 2026 Element Creations Ltd.
 *
 * SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial.
 * Please see LICENSE files in the repository root for full details.
 */

// Distinct from Element X's "ElementX_plugins": both plugin builds are loaded when this repository is
// included in Element X as a composite build (see docs/local_stack.md, layer 2).
rootProject.name = "ElementCallAndroid_plugins"

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
    versionCatalogs {
        create("libs") {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}
